 i = 0; i < p.size(); i++) {
      //      phash[p[i]-'a']++;
      //  }
      //  for(int i = 0; i < s.size(); i++) {
      //      shash[s[i]-'a']++;
      //      if(i+1 >= p.size()) {
      //          if(phash == shash) {
      //              v.push_back(i+1-p.size());
      //          }
      //          shash[s[i]-'a'-p.size()+1]--;
      //      }
      //  }
      //  for(auto i : v) {
      //   cout<<i<<" ";
      //  }
      //  cout<<en